/**
 * The canned reply the fake Gateway streams back for `engine.askStream`. A
 * single source, like `WHY_PEEK` in briefs.ts, so a spec asserts against
 * this constant rather than a copy of the literal.
 */
export const ASK_REPLY = "Got it — thanks for the attached context.";
